name: TEST
on: [push]

jobs:
  build:
    name: Build
    env:
      POPOPO: aaaaa
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1

      - name: Extract branch name
        run: |
          echo "##[set-output name=branch;]$(echo ${GITHUB_REF#refs/heads/})"
          echo $POPOPO
        id: extract_branch

      - name: second step
        uses: docker://devth/helm:latest
        env:
          POPOPO: ${{ steps.extract_branch.outputs.branch }}-$GITHUB_SHA
        with:
          entrypoint: /bin/bash
          args: -c "
            echo hogefuga &&
            echo $POPOPO &&
            echo $GITHUB_SHA &&
            echo ${{ steps.extract_branch.outputs.branch }}
            "
